<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Базовая тема без Action Bar для Compose -->
    <style name="Theme.Draft" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Опционально: цвета статус-бара и навигации -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/white</item>
    </style>
</resources>